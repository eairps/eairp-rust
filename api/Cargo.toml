[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.41.1", features = ["full"] }
axum = "0.8.0-alpha.1"
tower = "0.5.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# config
config = "0.14.1"
dotenv = "0.15.0"